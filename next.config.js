/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lmg.jj20.com', 'pic1.win4000.com', 't7.baidu.com'],
  },
}

module.exports = nextConfig
